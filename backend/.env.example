# Runtime configuration for the Family Finance Tracker API
# Copy this file to backend/.env and adjust values for your environment.

# Application
NODE_ENV=production
PORT=4000
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:5173

# Database configuration
# Use sqlite3 by default. To use PostgreSQL, change DATABASE_CLIENT to pg
# and set DATABASE_URL to a valid connection string.
DATABASE_CLIENT=sqlite3
DATABASE_URL=

# Authentication secrets
# Generate strong secrets for production deployments.
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Password hashing strength
BCRYPT_SALT_ROUNDS=10
